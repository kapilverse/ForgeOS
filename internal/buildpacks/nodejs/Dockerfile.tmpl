# Auto-generated by ForgeOS buildpack: Node.js
FROM node:20-alpine

WORKDIR /app

# Install deps first for layer caching.
COPY package*.json ./
RUN {{ if .UseYarn }}yarn install --frozen-lockfile{{ else if .HasPackageLock }}npm ci{{ else }}npm install{{ end }}

# Copy the rest of the source.
COPY . .

{{ if .IsTS -}}
RUN npm run build
{{- end }}

ENV NODE_ENV=production
ENV PORT={{ .Port }}
EXPOSE {{ .Port }}

CMD {{ .StartCmd }}
