# Auto-generated by ForgeOS buildpack: Go (multi-stage)
FROM golang:1.22-alpine AS build

WORKDIR /app

# Cache module downloads.
COPY go.mod go.sum* ./
RUN go mod download

COPY . .

# Build a static binary so the runtime image can be scratch-like.
RUN CGO_ENABLED=0 go build -o /out/app {{ .BuildTarget }}

# --- runtime ---
FROM alpine:latest

RUN apk add --no-cache ca-certificates

WORKDIR /app
COPY --from=build /out/app ./app

ENV PORT={{ .Port }}
EXPOSE {{ .Port }}

CMD ["./app"]
