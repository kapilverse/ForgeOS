# This file is intentionally empty: when a repo ships its own Dockerfile,
# ForgeOS runs it as-is. The builder skips template generation entirely for the
# "docker" language and uses the repo's Dockerfile directly.
