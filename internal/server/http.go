// Package server wires the chi router, middleware, and handlers into the
// ForgeOS HTTP API. Route registration lives in router.go; shared HTTP helpers
// live in the handlers package.
package server
